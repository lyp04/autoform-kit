<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Android 11+ package visibility: the capture flow enumerates camera handlers so it can
         grant each handler temporary access to the app-owned output URI before launching it. -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".SimplePhotoProvider"
            android:authorities="${applicationId}.photo"
            android:exported="false"
            android:grantUriPermissions="true" />
        <provider
            android:name=".UpdateApkProvider"
            android:authorities="${applicationId}.update"
            android:exported="false"
            android:grantUriPermissions="true" />
        <provider
            android:name=".SessionAuthProvider"
            android:authorities="${applicationId}.session"
            android:exported="true" />
        <!-- Cross-app session bus. exported so a peer's directed broadcast reaches us even when
             backgrounded / process-dead; the receiver only re-reads our own (allow-list-protected)
             token, so a forged broadcast is harmless. -->
        <receiver
            android:name=".SessionEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.autoformkit.app.SESSION_CHANGED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".CaptureActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize"
            android:screenOrientation="portrait"
            android:exported="false" />
        <activity
            android:name=".ScannerActivity"
            android:clearTaskOnLaunch="true"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:exported="false" />
        <!-- Browser pairing is isolated from MainActivity's launcher filter. The application-id
             scheme keeps release and side-by-side debug installs from competing for one link. -->
        <activity
            android:name=".PanelPairingEntryActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="pair"
                    android:path="/v1"
                    android:scheme="${applicationId}" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InternalMainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize"
            android:exported="false" />
        <!-- Keep the original public launcher component stable for in-place upgrades and existing
             home-screen shortcuts, but route it to the non-exported runtime Activity. -->
        <activity-alias
            android:name=".MainActivity"
            android:exported="true"
            android:targetActivity=".InternalMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>
